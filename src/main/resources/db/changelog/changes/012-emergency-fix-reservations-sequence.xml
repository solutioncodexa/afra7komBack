<?xml version="1.0" encoding="UTF-8"?>
<databaseChangeLog
        xmlns="http://www.liquibase.org/xml/ns/dbchangelog"
        xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
        xsi:schemaLocation="http://www.liquibase.org/xml/ns/dbchangelog
        http://www.liquibase.org/xml/ns/dbchangelog/dbchangelog-4.4.xsd">

    <changeSet id="012-emergency-fix-reservations-sequence" author="afra7kom" runOnChange="true">
        <!-- Emergency fix for reservations sequence - force update to next available ID -->
        <sql>
            -- Force reset the reservations sequence to the next available ID
            SELECT setval('reservations_id_seq', COALESCE((SELECT MAX(id) FROM reservations), 0) + 1, false);
        </sql>
        
        <!-- Also fix materiels sequence as backup -->
        <sql>
            SELECT setval('materiels_id_seq', COALESCE((SELECT MAX(id) FROM materiels), 0) + 1, false);
        </sql>
    </changeSet>

</databaseChangeLog>


